# 每日開發日誌：2026-01-23 (架構覺醒：從工具使用者到系統設計者)

## 📝 今日事件：全自動開發的「暈眩感」
- **場景**：開始使用 Firebase 架構新系統，對 CRUD、Auth 等技術完全授權給 AI 實作。
- **關鍵觀察**：
    - **非對稱開發**：雖然看不懂底層代碼（Firebase SDK、Token 驗證），但系統卻神奇地完成了。
    - **實驗性除錯**：在 Odoo XML 視圖與 CSS 衝突時，使用 AI 作為「動態探針」在瀏覽器直接驗證假設，成功釐清責任層級並回寫模組。
    - **平台工具的邊界**：從 Kintone 轉到 Odoo，發現與其在 UI 層硬碰硬（CSS 搞到生氣），不如直擊「資料庫層」，將原生欄位視為 Source of Truth，自訂模組僅作為表現層。

## 💡 關鍵決策與邏輯
1. **架構主導的 AI 授權 (Architecture-led Delegation)**：
    - 意識到「完全授權」並非放手，而是由人掌握：資料模型、業務流程、設計原則，讓 AI 僅補足 implementation detail。
2. **責任歸因與分層 (Responsibility Attribution)**：
    - 當 UI 出現問題時，先問「這屬於哪一層？」（View / CSS / Data），而非盲目嘗試。
3. **資料庫層的降維打擊**：
    - 懂資料庫背後的結構，才有能力在龐雜的 Odoo 系統中規劃出自由佈置的模組，避免被原生 UI 綁架。

## 💭 今日反思
- **升級的暈眩感**：那種「這東西我沒全部寫，但我知道它為什麼這樣長」的感覺，是從執行者跨向設計者的標誌。
- **後台外包的策略**：使用 Firebase 本質上是將「非核心風險」（驗證、防禦、基礎設施）外包給 Google，這是一種架構判斷。

---
> [!TIP]
> **今日金句**：我不是在學工具，我是在找資料真正站得住的位置。
